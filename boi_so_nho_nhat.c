#include<stdio.h>
int main(){
    int a,b,t,s[100];
    scanf("%d",&t);
    while(t--){
        int j=2;
        scanf("%d %d",&a,&b);
        for(int i=a;i<=b;i++){
            s[i]=i;
        }
        for()
    }
}